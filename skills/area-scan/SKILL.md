---
name: area-scan
description: >-
  구·동 단위 상업업무용 통건물 실거래 시세를 조회한다. "○○구 시세", "○○동 시세",
  "○○동 평당가 얼마", "강남구 동별 평단가", "○○동 평당 4~5억 거래" 같은 질문에 사용. deal-locator
  MCP 의 area_scan 도구를 호출하고, 동 평균 시세는 반드시 stats(국토부 원본 전수)로
  답한다. 특정 지번 한 건 조회나 카드 제작은 이 스킬이 아니다.
argument-hint: <구 또는 동> [최소억/평 최대억/평] [개월] [도로명]
---

# 지역 시세 스캔 (`/area-scan`)

`$ARGUMENTS` 를 아래 규칙으로 해석해 **deal-locator MCP 의 `area_scan` 도구**를 호출하고,
그 결과를 중개 실무 규칙에 맞게 정리해 답한다.

## 1) 인자 해석

- **첫 토큰(들) = 지역** — **구**(`강남구`·`강남`)면 구 모드, **동**(`군자동`·`성수동1가`)이면
  동 모드다. 그대로 `area` 에 넘기면 도구가 알아서 가른다. `광진구 군자동`처럼 둘 다
  있으면 동 모드. 동만 주면 엔진이 구를 역추적하므로, 결과의 `area.gu` 로 어느 구로
  잡혔는지 확인해 사용자에게 알린다.
- **숫자 2개** = 연면적 평당가 밴드(억/평) → `min_eok_per_pyeong`, `max_eok_per_pyeong`.
  하나만 있으면 방향에 맞춰(이상/이하) 한쪽만 채우고 다른 쪽은 0.
- **`N개월` / `최근 N`** = `months`. 지정 없으면 **12개월**로 호출한다. 24개월은
  범위가 넓어 '요즘 시세'가 흐려진다 — 표본이 모자라면(통건물 n<10) 그때 24개월로
  늘려 다시 부르고, 기간을 늘렸다는 사실을 밝힌다.
- **`○○길`·`○○로` 같은 도로명** = `road_contains`.
- **밴드가 없으면 min/max 를 생략(0)** — 이게 **동 전체 시세를 보는 기본 모드**다.

예시:
- `/area-scan 강남구` → `area_scan(area="강남구", months=12)` — 구 모드
- `/area-scan 군자동` → `area_scan(area="군자동", months=12)`
- `/area-scan 성수동1가 4.5 5.5` → `min_eok_per_pyeong=4.5, max_eok_per_pyeong=5.5`
- `/area-scan 군자동 연무장길` → `road_contains="연무장길"`

## 2) 답변 규칙 (반드시 지킬 것)

0. **동 시세 요약 한 줄은 생략 불가다.** 거래 목록(표)만 그리고 끝내지 마라. 사용자가
   가격대·도로명으로 물었든 목록을 요청했든, **표보다 먼저 `stats` 요약 줄이 나와야
   한다.** 표에 보이는 몇 건은 `limit` 에 잘린 일부이고, 그걸 눈으로 훑어 시세를
   가늠하면 틀린다(`coverage.truncated` 가 그 잘린 건수다).

1. **"이 동 시세 / 평당가"를 물으면 `stats` 로 답한다. `deals` 로 평균을 내지 마라.**
   - `stats` 는 가격대·도로명 필터와 무관하게 **표제부 역매칭 이전의 국토부 원본
     실거래(상업업무용, 해제·산출불가 제외)로 산출한 전수**라 편향이 없다.
   - `deals` 는 필터를 통과한 **개별 거래 목록**일 뿐이다 — 이걸 평균 내면 표본이 줄어
     왜곡된다.
   - 연면적 평단가 = `stats.gross`(평균 `mean`·중앙 `median`·흔한구간 `p25~p75`).
     대지 평단가 = `stats.land`.

2. **이 MCP 는 통건물만 취급한다 — 모든 값이 통건물 기준이다.**
   국토부 원본의 `유형` 컬럼이 둘로 갈리고, 도구가 **집합을 응답 전 단계에서 제외**한다.
   - **집합(구분상가)** = 건물 한 칸 거래. **취급하지 않는다.** 제외 건수는
     `coverage.jiphap_excluded` 에 담긴다.
   - **일반(통건물)** = 꼬마빌딩·사옥 등 한 채 통거래. 이것만 남는다.

   따라서 `stats.gross`(연면적 평단가)도 `stats.land`(대지 평단가)도 전부 통건물
   시세다. **"○○동 상가 한 칸 얼마"에는 이 도구로 답하지 마라** — 취급 범위 밖이니
   "이 도구는 통건물만 다룬다"고 밝히고 답을 지어내지 않는다.

   - **통건물 대표값은 `stats.land`(대지 평단가)** 다. 중개 실무에서 통건물 가격은
     대지 평단가로 말한다. `stats.gross` 는 보조로 덧붙인다.
   - `coverage.jiphap_excluded` 가 크다고 해서 표본이 부실한 게 아니다. 취급 범위를
     벗어난 거래를 뺀 것이니, 필요하면 "구분상가 N건은 취급 범위 밖이라 제외" 정도로만
     한 번 언급한다.

3. **추이는 `stats.quarterly`(분기)로 말한다 — 월 단위로 쪼개지 마라.**
   통건물 거래는 월 1~7건이라(대치동 실측: 22개월 중 10개월이 2건 이하) 월별
   중앙값은 한두 건이 좌우한다. 그래서 도구가 **분기로 묶어** 준다.
   - 각 분기의 `n` 을 함께 읽고, **n<3 인 분기는 추세 근거로 쓰지 마라** — 수치를
     인용하지 말고 '표본부족'으로 넘긴다.
   - 분기가 2개 이하면 추이를 말하지 말고 기간 전체 시세만 제시한다.
   - 분기당 표본이 10건 안팎이라 **등락 폭이 크다.** "올랐다/내렸다"를 단정하지 말고
     "분기별로는 이렇게 움직였다" 수준으로 전한다. 대형 거래 한 건이 분기값을 흔든다.

4. **대표값은 범위(min~max)가 아니라 흔한구간(p25~p75)으로 제시**한다. 초대형 빌딩
   한 건이 min/max 를 왜곡하므로, "대략 이 정도"를 말할 때는 흔한구간과 중앙값을 쓴다.
   평균(`mean`)이 중앙값(`median`)보다 크게 높으면 소수 고가 거래가 끌어올린 것이니
   **중앙값을 앞세운다.**

5. **`coverage` 모수를 함께 읽어 대표성을 밝힌다** — 표본 수(`stats.gross.n`·
   `stats.land.n`), 동내 전체 매매(`total_in_dong`), 해제 제외(`cancelled_count`),
   평단가 산출불가(`ppp_uncomputable`), 잘린 건수(`truncated`). **표본이 적으면
   (대략 n<10) "표본이 적어 참고용"임을 반드시 명시**한다. 통건물은 거래가 드물어
   `stats.land.n` 이 한 자릿수인 동이 흔하다 — 이때 대지 평단가를 단정하지 마라.

6. **개별 거래를 나열할 때는 각 건의 매칭 신뢰도와 '마스킹 미복원' 표기를 그대로 전한다.**
   `추정매칭`·`인접후보`를 확정처럼 말하지 않는다. 주소가 미복원된 거래는 "금액·면적은
   실측이나 지번은 미확정"임을 붙인다.

7. **`scope` 를 먼저 읽고 그 모드에 맞게 답한다.**
   - `scope='구'` — 구 전체 시세 + 분기 추이 + `by_dong`(동별 대지 평단가, 높은 순).
     **`deals` 는 항상 비어 있다**(역매칭 미실행). 이걸 "거래가 없다"로 오독하지 마라.
     개별 지번이 필요하면 **동으로 다시 조회**하겠다고 안내한다.
   - `scope='동'` — 기존대로 거래 목록까지.
   - `by_dong` 의 각 동도 `n` 을 함께 전한다. n<5 인 동은 순위를 단정하지 마라
     (한 건짜리 동이 1위로 올라올 수 있다).

8. **수치를 지어내지 않는다.** `status` 가 `OK` 가 아니면(NOT_FOUND 등) 그 사유를 그대로
   전하고 추측·보간하지 않는다.

9. 금액은 **반올림** 표기한다(절사 금지). 평단가는 도구가 준 `만원/평` 단위를 그대로 쓴다.
   1억원/평을 넘으면 `1억 5,067만원/평` 처럼 억 단위를 앞에 붙여 읽기 쉽게 한다.

## 3) 출력 형태

**순서를 지킨다 — 요약이 먼저, 목록은 나중.**

### 구 모드 (`scope='구'`)

1. **구 시세 요약(필수).** `○○구 통건물 최근 N개월 — 대지 평당 중앙 X · 흔한구간 Y~Z (n건)`
2. **분기 추이 한 줄** (아래 동 모드와 같은 규칙).
3. **동별 순위 표** — 대지 평단가 높은 순, `n` 병기. n<5 인 동은 `※표본적음`.
4. **마무리 한 줄** — "개별 거래·지번은 동으로 다시 조회"를 안내한다.
   `deals` 가 비어 있는 건 정상이지 거래가 없다는 뜻이 아니다.
5. **출처 한 줄.**

### 동 모드 (`scope='동'`)

1. **동 시세 요약(필수, 2줄).** 무엇의 시세인지 괄호로 밝힌다.

   ```
   ○○동 통건물 최근 N개월
   — 대지 평당 중앙 X만원/평 · 흔한구간 Y~Z (n건)
   — 연면적 평당 중앙 X만원/평 · 흔한구간 Y~Z (n건)
   ```

   `stats.land` 가 비어 있으면 그 사실을 한 마디로 밝히고 연면적 기준만 제시한다.

2. **분기 추이 한 줄** (`stats.quarterly` 가 3개 이상일 때만).

   ```
   분기 추이(대지 중앙) — 2025 Q3 1억 3,900 (n=10) · 2025 Q4 1억 5,400 (n=8) · …
   ```

   `n<3` 인 분기는 수치 대신 `표본부족`. 등락은 서술하되 단정하지 않는다.

3. **모수 한 줄.** `동내 통건물 매매 N건 중 해제 M건 제외 · 목록은 상위 K건(J건 생략)`.
   `coverage.jiphap_excluded` 가 있으면 `구분상가 P건은 취급 범위 밖` 을 덧붙인다.

4. **거래 목록(요청이 있을 때만).** 금액·거래일·주소·신뢰도. 표로 그려도 되지만
   **위 1·2 를 대신할 수 없다.**

5. **출처 한 줄** — 도구가 제공한 `source` 를 그대로 유지한다.
